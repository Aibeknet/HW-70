export interface IContact {
  id: string;
  name: string;
  phoneNumber: string;
  email: string;
  urlPhoto: string;
}

export interface IContactMutation {
  name: string;
  phoneNumber: string;
  email: string;
  urlPhoto: string;
}

